# CAP4630-Project1
To run this project, navigate to the /src directory in terminal and execute the PokemonColosseum.py script. The application will prompt you with inputs in the terminal window. When prompted for an input, enter your input then submit it by pressing ENTER.

This program uses the following module and library imports:

* import csv
* import ast
* import random
* import sys
* import copy